# name of workflow
name: Helllo

on:
  push:
    branches: [main]


# job is inside the workflow
jobs:

#we have name of runners
  greet:
    runs-on: ubuntu-latest

    steps:
      - name: say hello to users
        run: echo "Hello-Dost"
